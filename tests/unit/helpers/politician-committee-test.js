import { politicianCommittee } from 'political-report/helpers/politician-committee';
import { module, test } from 'qunit';

module('Unit | Helper | politician committee');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = politicianCommittee([42]);
  assert.ok(result);
});
